"use strict";
describe("Generic Interface", () => {
    it("should support", () => {
        const person = {
            name: "Abdul",
            age: 30,
            married: false
        };
        console.log(person.name);
        console.log(person.age);
        console.log(person.married);
    });
});
